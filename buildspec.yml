version: 0.2

#env:
#  variables:
#    FOO: "bar"
#  parameter-store:
#    ecr_ak: "Global-ECR-AK"
#    ecr_sk: "Global-ECR-SK"

phases:
  install:
    runtime-versions: 
        docker: 18
  pre_build:
    commands:
      - echo "authenticate to Amazon ECR(us-west-1)"
      - bash ./mirror/ECR_Auth.sh
  build:
    commands:
      - cd mirror; bash mirror-images.sh
  post_build:
    commands:
      - echo "post_build phase"
artifacts:
  # files: 
  discard-paths: yes
